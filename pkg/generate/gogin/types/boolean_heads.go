// booleanHeads enumerates the boolean-family PG type tokens.
//
// Const-only file — filefunc skips //ff annotations on const/var-only
// files.

package types

var booleanHeads = map[string]bool{
	"BOOLEAN": true,
	"BOOL":    true,
}
