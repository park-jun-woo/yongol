//ff:func feature=gen-fastapi type=command control=sequence
//ff:what Generate — FastAPI 백엔드 코드 생성 (미구현 스켈레톤)
package fastapi

import (
	"fmt"

	"github.com/park-jun-woo/yongol/pkg/yongol"
)

// Generate produces FastAPI backend artifacts from a parsed Fullstack.
// This is a placeholder — the actual implementation is planned but not yet built.
func Generate(fs *yongol.Fullstack, dir string) error {
	return fmt.Errorf("fastapi backend generation not yet implemented")
}
