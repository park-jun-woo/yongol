package service

// @auth "PublishTemplate" "template" {} "Forbidden" 403
// @get Template existing = Template.FindBySourceWorkflowID({SourceWorkflowID: request.source_workflow_id})
// @exists existing "Already published" 409
// @post Template template = Template.Create({SourceWorkflowID: request.source_workflow_id, OrgID: currentUser.OrgID, Title: request.title, Description: request.description, Category: request.category})
// @response { template: template }
func PublishTemplate() {}
