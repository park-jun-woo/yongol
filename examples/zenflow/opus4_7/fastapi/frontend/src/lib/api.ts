// Generated by yongol. Do not edit.
// Source of truth: specs/api/openapi.yaml (operationId-keyed).
import createClient from 'openapi-fetch'
import type { paths, operations } from '../types/api'

type PathOf<K extends keyof operations> =
  operations[K] extends { parameters: { path: infer P } } ? P : {}

type QueryOf<K extends keyof operations> =
  operations[K] extends { parameters: { query?: infer Q } } ? (Q extends undefined ? {} : Q) : {}

type BodyOf<K extends keyof operations> =
  operations[K] extends { requestBody: { content: { 'application/json': infer B } } }
    ? B extends Record<string, never> ? {} : B
  : operations[K] extends { requestBody: { content: { 'multipart/form-data': infer B } } }
    ? B extends Record<string, never> ? {} : B
    : {}

type Req<K extends keyof operations> =
  PathOf<K> & QueryOf<K> & BodyOf<K> extends infer R
    ? keyof R extends never ? void : R
    : void

type Res<K extends keyof operations> =
  operations[K] extends { responses: { 200: { content: { 'application/json': infer R } } } } ? R : void

const client = createClient<paths>({ baseUrl: '/api' })

export const api = {
  ActivateWorkflow: (args: Req<'ActivateWorkflow'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/workflows/{id}/activate', { params: { path }, body } as any).then(r => r.data as Res<'ActivateWorkflow'>)
  },
  AddAction: (args: Req<'AddAction'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/workflows/{id}/actions', { params: { path }, body } as any).then(r => r.data as Res<'AddAction'>)
  },
  ArchiveWorkflow: (args: Req<'ArchiveWorkflow'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/workflows/{id}/archive', { params: { path }, body } as any).then(r => r.data as Res<'ArchiveWorkflow'>)
  },
  AutoAssignWorkflow: (args: Req<'AutoAssignWorkflow'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/workflows/{id}/auto-assign', { params: { path }, body } as any).then(r => r.data as Res<'AutoAssignWorkflow'>)
  },
  CloneTemplate: (args: Req<'CloneTemplate'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/templates/{id}/clone', { params: { path }, body } as any).then(r => r.data as Res<'CloneTemplate'>)
  },
  CreateWebhook: (args: Req<'CreateWebhook'>) => {
    return client.POST('/webhooks', { body: args ?? {} } as any).then(r => r.data as Res<'CreateWebhook'>)
  },
  CreateWorkflow: (args: Req<'CreateWorkflow'>) => {
    return client.POST('/workflows', { body: args ?? {} } as any).then(r => r.data as Res<'CreateWorkflow'>)
  },
  CreateWorkflowVersion: (args: Req<'CreateWorkflowVersion'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/workflows/{id}/new-version', { params: { path }, body } as any).then(r => r.data as Res<'CreateWorkflowVersion'>)
  },
  DeleteSchedule: (args: Req<'DeleteSchedule'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.DELETE('/workflows/{id}/schedule', { params: { path }, body } as any).then(r => r.data as Res<'DeleteSchedule'>)
  },
  DeleteWebhook: (args: Req<'DeleteWebhook'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.DELETE('/webhooks/{id}', { params: { path }, body } as any).then(r => r.data as Res<'DeleteWebhook'>)
  },
  ExecuteWithReport: (args: Req<'ExecuteWithReport'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/workflows/{id}/execute-with-report', { params: { path }, body } as any).then(r => r.data as Res<'ExecuteWithReport'>)
  },
  ExecuteWorkflow: (args: Req<'ExecuteWorkflow'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/workflows/{id}/execute', { params: { path }, body } as any).then(r => r.data as Res<'ExecuteWorkflow'>)
  },
  GetAuditLog: (args: Req<'GetAuditLog'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const query: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (v == null) continue
        if (!(k in path)) query[k] = v
      }
    }
    return client.GET('/audit-logs/{id}', { params: { path, query } } as any).then(r => r.data as Res<'GetAuditLog'>)
  },
  GetDashboard: (args?: Req<'GetDashboard'>) => {
    return client.GET('/dashboard', { params: { query: args ?? {} } } as any).then(r => r.data as Res<'GetDashboard'>)
  },
  GetExecutionDetail: (args: Req<'GetExecutionDetail'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const query: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (v == null) continue
        if (!(k in path)) query[k] = v
      }
    }
    return client.GET('/execution-logs/{id}/detail', { params: { path, query } } as any).then(r => r.data as Res<'GetExecutionDetail'>)
  },
  GetExecutionReport: (args: Req<'GetExecutionReport'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const query: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (v == null) continue
        if (!(k in path)) query[k] = v
      }
    }
    return client.GET('/execution-logs/{id}/report', { params: { path, query } } as any).then(r => r.data as Res<'GetExecutionReport'>)
  },
  GetRecentAuditLogs: (args?: Req<'GetRecentAuditLogs'>) => {
    return client.GET('/audit-logs/recent', { params: { query: args ?? {} } } as any).then(r => r.data as Res<'GetRecentAuditLogs'>)
  },
  GetSchedule: (args: Req<'GetSchedule'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const query: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (v == null) continue
        if (!(k in path)) query[k] = v
      }
    }
    return client.GET('/workflows/{id}/schedule', { params: { path, query } } as any).then(r => r.data as Res<'GetSchedule'>)
  },
  GetTemplate: (args: Req<'GetTemplate'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const query: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (v == null) continue
        if (!(k in path)) query[k] = v
      }
    }
    return client.GET('/templates/{id}', { params: { path, query } } as any).then(r => r.data as Res<'GetTemplate'>)
  },
  GetWorkflow: (args: Req<'GetWorkflow'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const query: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (v == null) continue
        if (!(k in path)) query[k] = v
      }
    }
    return client.GET('/workflows/{id}', { params: { path, query } } as any).then(r => r.data as Res<'GetWorkflow'>)
  },
  ListAuditLogs: (args?: Req<'ListAuditLogs'>) => {
    return client.GET('/audit-logs', { params: { query: args ?? {} } } as any).then(r => r.data as Res<'ListAuditLogs'>)
  },
  ListExecutionLogs: (args: Req<'ListExecutionLogs'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const query: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (v == null) continue
        if (!(k in path)) query[k] = v
      }
    }
    return client.GET('/workflows/{id}/execution-logs', { params: { path, query } } as any).then(r => r.data as Res<'ListExecutionLogs'>)
  },
  ListTemplates: (args?: Req<'ListTemplates'>) => {
    return client.GET('/templates', { params: { query: args ?? {} } } as any).then(r => r.data as Res<'ListTemplates'>)
  },
  ListWebhooks: (args?: Req<'ListWebhooks'>) => {
    return client.GET('/webhooks', { params: { query: args ?? {} } } as any).then(r => r.data as Res<'ListWebhooks'>)
  },
  ListWorkflowVersions: (args: Req<'ListWorkflowVersions'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const query: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (v == null) continue
        if (!(k in path)) query[k] = v
      }
    }
    return client.GET('/workflows/{id}/versions', { params: { path, query } } as any).then(r => r.data as Res<'ListWorkflowVersions'>)
  },
  ListWorkflows: (args?: Req<'ListWorkflows'>) => {
    return client.GET('/workflows', { params: { query: args ?? {} } } as any).then(r => r.data as Res<'ListWorkflows'>)
  },
  Login: (args: Req<'Login'>) => {
    return client.POST('/auth/login', { body: args ?? {} } as any).then(r => r.data as Res<'Login'>)
  },
  PauseWorkflow: (args: Req<'PauseWorkflow'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/workflows/{id}/pause', { params: { path }, body } as any).then(r => r.data as Res<'PauseWorkflow'>)
  },
  PublishTemplate: (args: Req<'PublishTemplate'>) => {
    return client.POST('/templates', { body: args ?? {} } as any).then(r => r.data as Res<'PublishTemplate'>)
  },
  SaveWorkflowActions: (args: Req<'SaveWorkflowActions'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.PUT('/workflows/{id}/actions', { params: { path }, body } as any).then(r => r.data as Res<'SaveWorkflowActions'>)
  },
  SetSchedule: (args: Req<'SetSchedule'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/workflows/{id}/schedule', { params: { path }, body } as any).then(r => r.data as Res<'SetSchedule'>)
  },
  VerifyOrgAddress: (args: Req<'VerifyOrgAddress'>) => {
    const a = args as any
    const path: Record<string, any> = {}
    if (a && a['id'] !== undefined) path['id'] = a['id']
    const body: Record<string, any> = {}
    if (a) {
      for (const [k, v] of Object.entries(a)) {
        if (!(k in path)) body[k] = v
      }
    }
    return client.POST('/organizations/{id}/verify-address', { params: { path }, body } as any).then(r => r.data as Res<'VerifyOrgAddress'>)
  },
}
