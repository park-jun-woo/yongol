package service

// @auth "ListTemplates" "template" {} "Forbidden"
// @get []Template items = Template.ListCursor({Cursor: request.cursor, PerPage: request.per_page, FilterCategory: request.category})
// @response { items: items }
func ListTemplates() {}
