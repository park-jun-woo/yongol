package template

// @state-neutral
// @get Workflow workflow = Workflow.FindByID({ID: request.workflow_id})
// @empty workflow "Workflow not found" 404
// @auth "PublishTemplate" "workflow" {} "Permission denied"
// @post Template _ = Template.Create({WorkflowID: workflow.ID, Title: workflow.Title, Description: request.description, Category: request.category})

// @response {}
func PublishTemplate() {}
