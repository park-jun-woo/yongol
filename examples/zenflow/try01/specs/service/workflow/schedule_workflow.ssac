package workflow

// @state-neutral
// @get Workflow workflow = Workflow.FindByID({ID: request.id})
// @empty workflow "Workflow not found" 404
// @auth "ScheduleWorkflow" "workflow" {} "Permission denied"
// @put Workflow.SetSchedule({ID: workflow.ID, Cron: request.cron})

// @response {}
func ScheduleWorkflow() {}
