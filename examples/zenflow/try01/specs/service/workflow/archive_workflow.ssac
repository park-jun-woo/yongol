package service

// @auth "ArchiveWorkflow" "workflow" {ResourceID: request.id} "Forbidden"
// @get Workflow wf = Workflow.FindByID({ID: request.id})
// @empty wf "Workflow not found" 404
// @state workflow {status: wf.Status} "ArchiveWorkflow" "Cannot archive workflow" 409
// @put Workflow.UpdateStatus({ID: wf.ID, Status: "archived"})
// @get Workflow updatedWf = Workflow.FindByID({ID: wf.ID})
// @response { workflow: updatedWf }
func ArchiveWorkflow() {}
