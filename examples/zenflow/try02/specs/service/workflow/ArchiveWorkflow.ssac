package workflow

// @auth "ArchiveWorkflow" "workflow" {ResourceID: request.id} "Forbidden"
// @get Workflow wf = Workflow.FindByID({ID: request.id})
// @empty wf "Workflow not found"
// @state workflow {status: wf.Status} "ArchiveWorkflow" "Invalid state transition"
// @put Workflow.UpdateStatus({ID: request.id, Status: "archived"})
// @get Workflow updatedWf = Workflow.FindByID({ID: request.id})
// @response updatedWf
func ArchiveWorkflow() {}
