package workflow

// @auth "PauseWorkflow" "workflow" {ResourceID: request.id} "Forbidden"
// @get Workflow wf = Workflow.FindByID({ID: request.id})
// @empty wf "Workflow not found"
// @state workflow {status: wf.Status} "PauseWorkflow" "Invalid state transition"
// @put Workflow.UpdateStatus({ID: request.id, Status: "paused"})
// @get Workflow updatedWf = Workflow.FindByID({ID: request.id})
// @response updatedWf
func PauseWorkflow() {}
