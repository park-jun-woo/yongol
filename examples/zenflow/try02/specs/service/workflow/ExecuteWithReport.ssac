package workflow

import (
	"github.com/park-jun-woo/zenflow/internal/billing"
	"github.com/park-jun-woo/zenflow/internal/report"
	"github.com/park-jun-woo/ssac/pkg/file"
)

// @auth "ExecuteWithReport" "workflow" {ResourceID: request.id} "Forbidden"
// @get Workflow wf = Workflow.FindByID({ID: request.id})
// @empty wf "Workflow not found"
// @state workflow {status: wf.Status} "ExecuteWithReport" "Workflow is not active"
// @call billing.DeductCreditResponse _ = billing.DeductCredit({OrgID: currentUser.OrgID, Amount: 1})
// @post ExecutionLog log = ExecutionLog.Create({WorkflowID: request.id, OrgID: currentUser.OrgID, Status: "success", CreditsSpent: 1})
// @call report.GenerateReportResponse rpt = report.GenerateReport({WorkflowID: request.id, ActionCount: 0, Status: "success"})
// @call file.UploadResponse _ = file.Upload({Key: rpt.Key, Body: rpt.Content})
// @put ExecutionLog.SetReportKey({ID: log.ID, ReportKey: rpt.Key})
// @get ExecutionLog updatedLog = ExecutionLog.FindByID({ID: log.ID})
// @response updatedLog
func ExecuteWithReport() {}
