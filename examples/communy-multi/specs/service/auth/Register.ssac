package service

import "github.com/park-jun-woo/ssac/pkg/auth"

// @call HashedPasswordResponse hashed = auth.HashPassword({Password: request.password})
// @post User user = User.Create({Email: request.email, PasswordHash: hashed.HashedPassword, Nickname: request.nickname, Role: "user"})
// @response { id: user.ID, email: user.Email, nickname: user.Nickname, role: user.Role }
func Register() {}
